enum Filter {
  ALL
  COMPLETED
  ACTIVE
}

type Todo {
  id: Int!
  text: String!
  completed: Boolean!
}

type Query {
  allTodos: [Todo!]
}

type Mutation {
  addTodo(text: String!): Todo!
  setFilter(filter: Filter): Filter!
  toggleCompleted(id: Int!): Todo!
}
