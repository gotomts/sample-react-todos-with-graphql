enum Filter {
  ALL
  COMPLETED
  ACTIVE
}

type Todo {
  id: ID!
  text: String!
  completed: Boolean!
}

type Query {
  allTodos: [Todo!]
}

type Mutation {
  addTodo(text: String!): Todo!
  setFilter(filter: Filter): Filter!
  toggleCompleted(id: ID!): Todo!
}
