enum Filter {
  ALL
  COMPLETED
  ACTIVE
}

type Query {
  allTodos: [Todo!]
}

type Mutation {
  addTodo(text: String!): Todo!
  setFilter(filter: Filter): Filter!
}

type Todo {
  id: ID!
  text: String!
  completed: Boolean!
}
